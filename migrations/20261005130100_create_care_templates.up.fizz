create_table("care_templates") {
 t.Column("id", "uuid", {primary: true})
 t.Column("user_id", "uuid", {null: true})
 t.Column("name", "string", {size: 200})
 t.Column("content", "text", {})
 t.Timestamps()
}
add_index("care_templates", ["user_id"], {})
