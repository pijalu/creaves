create_table("users"){
    t.Column("id", "uuid", {"primary": true})
    t.Column("email", "string", {})
    t.Column("admin", "bool", { "default":false})
    t.Column("approved", "bool", { "default":false})
    t.Column("password_hash", "string", {})

}
