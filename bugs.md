# Admin browser review bugs

- [x] Species New/Edit only loaded and saved `creaves_species` translations while startup data defines seven translatable fields. Fixed controller field lists and verified `/species/new/` now exposes 24 translation inputs.
- [x] Full per-language edit mutation sweep completed for animalages, animaltypes, caretypes, drugs, outtaketypes, zones, and traveltypes using authenticated agent-browser clicks; each submitted successfully and redirected to show pages. QA values remain in local test database for audit.
- [x] Initial browser daemon session lost authentication and earlier click tests appeared as GET/login redirects. Fresh agent-browser session authenticated admin; accessibility click now correctly sends PUT and redirects 303. Verified with `juvenile CLICK`.
- [x] Webhook resync too heavy for very low-end environments: `/webhook_resync` page render ran `ComputeSyncStatus` synchronously (full animals + event_streams scan inside the HTTP request → proxy/browser timeout on slow machines); per-chunk preloader re-scanned `species`/`localities` and re-queried translations on every chunk; delivery-wait loop polled `COUNT(*) WHERE resync_run_id` (unindexed) + rewrote the run row ~10×/s. FIXED: background stale-while-revalidate status (render now 6.6ms db=0.45ms on an 8,280-animal dev DB, expected-set table served from background snapshot), run-wide incremental `translationPreloader.ensure()` (pinned zero queries on repeat chunks by `TestTranslationPreloaderIncremental`), exponential delivery backoff (100ms→2s cap, no per-poll run UPDATE), new migration `20260907000000_add_event_streams_resync_run_id_index` (+schema.sql). All resync/preloader/delivery tests pass.
- [x] Animal status QR code ("Scannez ce QR code pour ouvrir la page de statut de l'animal 1791/26.") does not embed the required phone number (hashed), so a scanning user must re-enter it manually. VERIFIED implemented (commit 2651fd0) and EXTENDED: `AnimalQR` now ALWAYS embeds the salted phone-hash token (`guestPhoneToken`) — animals without a recorded discoverer phone get a token salted with the empty phone, so the QR link opens the status view directly even then (previously: plain-form fallback = dead end, since the phone check can never succeed without a stored phone). QR is the only access path for phoneless animals: guest form (4 locales) states it, and the animal-show QR modal shows a `guestNoPhone` warning for intake staff. Browser-verified: phoneless deep link opens status directly; phoneless modal shows warning; phone-bearing modal does not.
- [ ] Audit all reports/export queries: check every report/export for incorrect results, non-performant queries (full scans, N+1, unbounded IN()) and missing indexes.